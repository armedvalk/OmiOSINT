on:
    jobs:
      my_job:
        runs-on: ubuntu-latest
        steps:
          - name: Write secret to file
            run: |
              echo "${{ secrets.OSINT_API_1 }}" > secret.txt
            env:
              MY_SECRET_KEY: ${{ secrets.OSINT_API_1 }}
